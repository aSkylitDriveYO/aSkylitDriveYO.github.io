{
  "name": "test_sass_portfolio",
  "version": "1.0.0",
  "description": "Test Sass Portfolio",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass -w scss/ -o dist/css/ --recursive"
  },
  "author": "Chris Ortega",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^8.0.0"
  }
}
